class Solution:
    def singleNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        s=collections.Counter(nums)
        for i in list(s):
            if s[i] == 1:
                return i
            
        
